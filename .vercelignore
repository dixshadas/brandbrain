# The marketing site (this Vercel project) deploys only web/.
# The FastAPI backend, docs, tests and tooling are not part of the static deploy.
# The interactive prototype (prototype/) is a separate, independently-deployed
# Vercel project — it must never ship as part of this one. See DEPLOY.md.
src/
tests/
migrations/
contracts/
docs/
scripts/
alembic.ini
pyproject.toml
Dockerfile
docker-compose.yml
Makefile
setup-github.sh
*.py
.DS_Store
prototype/
